<%= form_for [@topic, @slide] do |f| %>
  <% if slide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(slide.errors.count, "error") %> prohibited this slide from being saved:</h2>

      <ul>
      <% slide.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :htmlBody %>
    <%= f.text_area :htmlBody %>
  </div>

  <div class="field">
    <%= f.label :topic_id %>
    <%= f.text_field :topic_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
